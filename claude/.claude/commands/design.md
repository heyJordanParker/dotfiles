---
description: Design UI components with intentional, anti-slop thinking
argument-hint: [design task]
---

Use the `design-thinking` skill.

$ARGUMENTS

If no task above, ask what to design. Explore existing patterns first.
