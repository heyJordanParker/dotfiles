• Read the FULL file first (not snippets via Grep)
• List what EXISTS before proposing changes
• Preserve unmentioned functionality - iterate, don't rewrite
• Check basics first: typos, wrong names, wrong values
• Understand root cause before changing code
