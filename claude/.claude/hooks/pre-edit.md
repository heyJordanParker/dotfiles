Before this edit, verify:

□ Have you READ this specific file in this session? (Grep snippets don't count)
□ Can you describe what the existing code does? If not, read it now.
□ What existing behavior must be preserved? List it explicitly.
□ Is this a root cause fix, or are you patching symptoms?
□ Check basics first: typos, wrong names (use /naming skill), wrong values.

If you haven't read the file, read it before editing.
